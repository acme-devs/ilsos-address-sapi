# Default environment
env: local

# The RAML API and autodiscovery
api:  
  group: "${restconnector.api.spec.group}"
  artifact: "${restconnector.api.spec.artifact}"
  version: "${restconnector.api.spec.version}"
  spec: "resource::${api.group}:${api.artifact}:${api.version}:raml:zip:api.raml"
  id: "0"
  grant_type : "client_credentials"
  client_id: "AraUal8FJpUpfGbbcDgNf6UeHATsYBaE"
  client_secret: "Hd8c1Dzyexemtqi4"
  code: "EyQPFYVI"
  uspsDomain: "api.usps.com"
  uspsPort: "443"
  tokenUrl: "/oauth2/v3/token"
  addressUrl: "/addresses/v3/address"